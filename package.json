{
  "name": "xhermes",
  "version": "0.0.1",
  "description": "xHermes — talk to your Hermes agent from the terminal.",
  "type": "module",
  "bin": {
    "xhermes": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.6",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  },
  "engines": { "node": ">=20" }
}
